# (1) Install selenium
pip install selenium

# (2) Then install drivers
chrome: https://sites.google.com/a/chromium.org/chromedriver/downloads

make sure to check the correct chrome version - by googling & download the appropriate driver (chromedriver_win32.zip)

# (3) Install pytest
pip install -U pytest

# (4) Install pytest-html
pip install pytest-html